# Пример конфигурации для миграции MySQL → ClickHouse
# Скопируйте в .env и заполните своими значениями.

# === MySQL (источник) ===
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=retail

# === ClickHouse (приёмник) ===
CH_HOST=localhost
CH_PORT=8123
CH_USER=default
CH_PASSWORD=
CH_DATABASE=default

# === Опционально: миграция и память ClickHouse ===
# Размер батча при копировании (по умолчанию 2000; для очень больших таблиц — 500):
# MIGRATE_BATCH_SIZE=2000
# MIGRATE_BATCH_SIZE_LARGE=500
# Лимиты памяти ClickHouse при миграции больших таблиц:
# CH_MEMORY_HIGH_GB=4.5
# CH_MEMORY_TARGET_GB=3.5
# CH_MEMORY_POLL_MS=2000
# CH_MEMORY_MAX_WAIT_MS=600000

# Порт бэкенд-сервера дашбордов
# PORT=3001
